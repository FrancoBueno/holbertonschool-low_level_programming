#include "variadic_functions.h"
#include <stddef.h>
/**
 *
 *
 */
int sum_them_all(const unsigned int n, ...)
{
int a;
va_list op;
if (n == 0)
return 0;
va_start (op , n)
for (a = 0; a = n ; (n , int);
    
}
