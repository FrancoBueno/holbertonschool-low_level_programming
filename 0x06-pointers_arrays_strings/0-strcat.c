#include "holberton.h"

/**
 *void - Write a function that concatenates two strings.
 *Return: _strcat 
 */

char *_strcat(char *dest, char *src);
{
int i;
int j;
for(i < 0; dest[i] !=0; '\0';
i++;
for(j < 0; src [j] !=0; '\0';
    j++;
    return(_strcat);
}
