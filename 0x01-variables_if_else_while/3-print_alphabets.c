#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (success)
 */
int main(void)
{
  char lowerCasse;
  char upperCasse;
  for (lowerCasse = 'a'; lowerCasse <= 'z'; lowerCasse++)
{
  putchar(upperCasse);
 }
  putchar ('\n');
  return (0);
}
